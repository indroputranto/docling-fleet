# Web framework
flask>=3.0.0
werkzeug>=3.0.0

# Document parsing (existing pipeline + upload feature)
mammoth==1.6.0
python-docx>=1.1.0
openpyxl>=3.1.0
PyMuPDF>=1.24.0      # PDF text extraction — handles ligatures and custom font encodings
pdfplumber>=0.11.0   # PDF fallback extractor

# AI / LLM
openai>=1.30.0
anthropic>=0.28.0

# Vector database
pinecone>=4.0.0

# Token counting (used in embedding uploader)
tiktoken>=0.7.0

# Database & auth
flask-sqlalchemy>=3.1.0
PyJWT>=2.8.0
psycopg2-binary>=2.9.0  # PostgreSQL driver (required for Vercel / production)

# Environment & config
python-dotenv>=1.0.0

# Object storage (DigitalOcean Spaces / S3-compatible)
# minio is used instead of boto3 — it is fully S3-compatible and installs at
# ~1 MB vs ~90 MB for boto3+botocore, keeping the Vercel bundle well within limits.
minio>=7.2.0

# Utilities (existing pipeline)
pathlib2==2.3.7
typing-extensions>=4.8.0
requests>=2.31.0
PyYAML>=6.0.0
