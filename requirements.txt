# Web framework
flask>=3.0.0
werkzeug>=3.0.0

# Document parsing (existing pipeline + upload feature)
mammoth==1.6.0
python-docx>=1.1.0
openpyxl>=3.1.0
PyMuPDF>=1.24.0      # PDF text extraction — handles ligatures and custom font encodings
pdfplumber>=0.11.0   # PDF fallback extractor

# OCR pipeline for image-source PDFs (multifunction-printer scans, photos)
# ocrmypdf wraps Tesseract; tesseract itself must be installed at the OS
# level — see deploy.sh / docling-flask.service for Droplets:
#     apt-get install -y tesseract-ocr tesseract-ocr-eng
# On Vercel (read-only filesystem, no system tesseract) the OCR pre-pass
# detects the missing binary and gracefully falls back to the existing
# extraction path with a logged warning.
ocrmypdf>=16.0.0
opencv-python-headless>=4.10.0  # raster strikethrough detection (no GUI deps)

# AI / LLM
openai>=1.30.0
anthropic>=0.28.0

# Vector database
pinecone>=4.0.0

# Token counting (used in embedding uploader)
tiktoken>=0.7.0

# Database & auth
flask-sqlalchemy>=3.1.0
PyJWT>=2.8.0
psycopg2-binary>=2.9.0  # PostgreSQL driver (required for Vercel / production)

# Environment & config
python-dotenv>=1.0.0

# Object storage (DigitalOcean Spaces / S3-compatible)
# boto3 handles uploads/downloads/deletes/presigned URLs. DO Spaces requires
# SigV4 + virtual-hosted addressing (which MinIO did not always sign correctly).
boto3>=1.34.0

# Utilities (existing pipeline)
pathlib2==2.3.7
typing-extensions>=4.8.0
requests>=2.31.0
PyYAML>=6.0.0
